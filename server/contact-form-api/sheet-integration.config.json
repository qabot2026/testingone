{
  "extraColumnMappings": [
    {
      "tab": "Sheet1",
      "entries": [
        {
          "startColumn": "B",
          "valueFrom": "session_params.coursename",
          "shiftIfOccupied": true,
          "_note": "true = shift right if startColumn is used by lead row; false = force that column (may overwrite)."
        }
      ]
    }
  ]
}
