{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "rootDirectory": "server/chatbot-api",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": ["**", "api-base.config.js"]
  },
  "deploy": {
    "healthcheckPath": "/health",
    "startCommand": "node index.mjs"
  }
}
